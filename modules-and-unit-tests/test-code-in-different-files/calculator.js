export const getSquaredArray = (nums) => nums.map(num => num**2);

export const getOddNumbers = (nums) => nums.filter(num => num % 2 === 1);

export default function sum(num1, num2) {
  return num1 + num2;
}